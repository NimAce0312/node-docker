# Minimal OpenSSL configuration file for mongosh

openssl_conf = default_conf

[ default_conf ]
ssl_conf = ssl_section

[ ssl_section ]
system_default = system_default_sect

[ system_default_sect ]
# You can set the cipher string as needed; here we use a common default.
CipherString = DEFAULT@SECLEVEL=2